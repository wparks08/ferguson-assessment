/**
 * URLs for communicating with the API.
 */
export const urls = {
    v1: {
        students: {
            find: "/v1/students",
            create: "/v1/students",
        },
        states: {
            find: "/v1/states",
        },
    },
};
